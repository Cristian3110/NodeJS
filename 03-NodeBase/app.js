/**
 *  working
 */

console.clear();
console.log(`
==========================
        Tabla del 5
==========================`);

for (let n = 0; n <= 10; n++) {
	r = n * 5;
	console.log(`5 x ${n} = ${r}`);
}

/**
 * Ejemplo de Fernando
 *
 * const base = 5;
 * for(let i=0; i<=10; i++){
 * console.log(`${base} x ${i} = ${base * i})}
 *
 */
